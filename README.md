# SICEI API - AWS Cloud Foundations

Proyecto de API REST para la gestión de Alumnos y Profesores, desplegado en AWS EC2.

## Instalación Local
1. Instalar dependencias: `pip install -r requirements.txt`
2. Ejecutar: `uvicorn main:app --reload`

## Endpoints Principales
- GET /alumnos
- GET /profesores